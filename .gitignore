# macOS metadata
.DS_Store

# Claude Code local context
.claude/
CLAUDE.md

# Python
__pycache__/
*.pyc
*.pyo
*.py
generate_website.py
scripts/


# Database credentials — never commit this
config.py

# Cached images and data (regenerated automatically)
img_cache/
cache.json

# update_progeny.py state — races already drafted (local only)
progeny_seen.json

# scratch files
*.pdf
*.docx
*.eps
*.xlsx

stallion_data.xlsx

stallion PPs/
stuff/

# Fonts — not used by the generator
fonts/

# Logo files — embedded as base64 at build time, not needed in repo
logos/
